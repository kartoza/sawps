import React from 'react';
import { render, screen } from '@testing-library/react';
import 'jest-canvas-mock';

import SpeciesChart from '.'

describe("test chart components for different species, testing chartjs react lib",()=>{
    it("renders the chart component",()=>{
        //render(<SpeciesChart/>)
        //let chartComponent = screen.getByTestId("species-chart")
        //expect(chartComponent).toBeInTheDocument()
    })
})